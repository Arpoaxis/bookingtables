<web-app id="WebApp_ID" version="6.0" xmlns="https://jakarta.ee/xml/ns/jakartaee" 
			xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
			xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd">
	<display-name>RestaurantBooking</display-name>
	
	<welcome-file-list>
    <welcome-file>index</welcome-file>
</welcome-file-list>
	
	<listener>
		<listener-class>com.restaurant.listeners.DatabaseInitializer</listener-class>
	</listener>
	
	<context-param>
	    <param-name>resetDatabaseOnStartup</param-name>
	    <param-value>true</param-value>
	</context-param>

	<filter>
  		<filter-name>EncodingFilter</filter-name>
  		<filter-class>com.restaurant.filter.EncodingFilter</filter-class>
  		<init-param>
    		<param-name>encoding</param-name>
    		<param-value>UTF-8</param-value>
  		</init-param>
	</filter>

	<filter-mapping>
	    <filter-name>EncodingFilter</filter-name>
	    <url-pattern>/jsp/*</url-pattern>
	    <url-pattern>/admin/*</url-pattern>
	    <url-pattern>/login</url-pattern>
	    <url-pattern>/register</url-pattern>
	    <url-pattern>/logout</url-pattern>
  </filter-mapping>
	<session-config>
    	<session-timeout>30</session-timeout>

    	<!-- cookie settings first -->
    	<cookie-config>
        	<http-only>true</http-only>
        	<!-- secure=true for HTTPS; false is OK for local http:// dev -->
        	<secure>false</secure>
    	</cookie-config>

    	<!-- then tracking mode -->
    	<tracking-mode>COOKIE</tracking-mode>
	</session-config>
	<filter>
    	<filter-name>CSRFFilter</filter-name>
    	<filter-class>com.restaurant.filter.CSRFFilter</filter-class>
	</filter>
	<filter-mapping>
    	<filter-name>CSRFFilter</filter-name>
    	<url-pattern>/admin/*</url-pattern>
	</filter-mapping>
	
  

</web-app>